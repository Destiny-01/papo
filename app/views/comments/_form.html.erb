<%= simple_form_for([@shop, @shop.comments.build]) do |f| %>
    <%= f.input :name %>
    <%= f.input :response, input_html: { class: "textarea"} %>
    <%= f.button :submit, 'Leave a reply', class: "btn btn-primary" %>
<% end %>